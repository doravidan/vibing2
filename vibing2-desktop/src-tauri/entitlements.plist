<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Enable hardened runtime for security -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required for some frameworks) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation to allow loading of frameworks -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access -->
    <key>com.apple.security.network.client</key>
    <true/>

    <key>com.apple.security.network.server</key>
    <true/>

    <!-- File access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Allow app to access user's home directory for project storage -->
    <key>com.apple.security.temporary-exception.files.home-relative-path.read-write</key>
    <array>
        <string>/</string>
    </array>

    <!-- Printing support (if needed for export features) -->
    <key>com.apple.security.print</key>
    <true/>

    <!-- Camera and microphone (if implementing voice input) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Bluetooth (optional, for peripheral support) -->
    <key>com.apple.security.device.bluetooth</key>
    <false/>

    <!-- USB access (optional) -->
    <key>com.apple.security.device.usb</key>
    <false/>
</dict>
</plist>
